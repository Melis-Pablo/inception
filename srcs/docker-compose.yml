version: "3"

services:
  nginx:
    container_name: nginx
    networks:
      - inception_network

  mariadb:
    container_name: mariadb
    networks:
      - inception_network

  wordpress:
    container_name: wordpress
    networks:
      - inception_network

networks:
  inception_network:
    driver: bridge
